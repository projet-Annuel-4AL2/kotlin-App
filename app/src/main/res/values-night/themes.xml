<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PA4A" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/black</item>

        <item name="android:windowBackground">@color/background_primary</item>
        <!-- Couleur de fond pour les vues -->
        <item name="android:colorBackground">@color/background_primary</item>
        <!-- Couleur de fond pour les cartes -->
        <!--<item name="android:colorCardBackground">@color:card_background</item> -->
        <!-- Texte principal -->
        <item name="android:textColorPrimary">@color/text_primary</item>
        <!-- Texte secondaire -->
        <item name="android:textColorSecondary">@color/text_secondary</item>
        <!-- Texte désactivé -->
        <item name="android:textColorTertiary">@color/text_disabled</item>
        <!-- Couleur d'accent -->
        <item name="colorAccent">@color/accent_blue</item>
        <!-- Couleur d'accent de notification -->
       <!-- <item name="notificationIconBackground">@color/notification_red</item> -->
    </style>
</resources>